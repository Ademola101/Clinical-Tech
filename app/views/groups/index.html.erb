<h2 class = "d-flex justify-content-center align-items-center m-4 fw-bold">

<div class = "d-flex ml-4 justify-content-center align-items-center">


Patients Groups
</h2><div class = "m-4 d-flex ml-4 justify-content-around align-items-center" >
<% if @groups%>

<p>
<% @groups.each do |group|%>
<div>

<%=link_to(group.name, group, class:"fw-bold text-decoration-none text-secondary fs-3")%>

<div>
<%= image_tag group.thumbnail, class:"img_res" if group.thumbnail.present? %>
</div>
</div>

<div>

<%= group.icon%>
</div>

<%end%>
</p>

<% else%>

<p> You have not added any group</p>
</div>

<%end%>


</div>

<div class = "d-flex ml-4 flex-column justify-content-around align-items-center">
<p><%= link_to('Add new group', new_group_path,class:"fw-bold text-decoration-none text-secondary"	)%> </p>
<p> <%= link_to("Back", :back, class:"fw-bold text-decoration-none text-secondary")%> </p>

</div>
